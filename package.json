{
  "name": "option-and-friends",
  "version": "1.0.1",
  "description": "Rust-inspired data structures for TypeScript",
  "main": "dist/index.js",
  "author": "Alice Pote",
  "license": "MIT",
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.0.6",
    "prettier": "^2.3.2",
    "ts-jest": "^27.0.5",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "rm -rf dist && rm -f .tsbuildinfo && tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "fmt": "prettier --write --no-semi src/*.ts"
  }
}
